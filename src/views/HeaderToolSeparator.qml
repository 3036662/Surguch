import QtQuick
import QtQuick.Controls

ToolSeparator {
    contentItem: Rectangle {
                    implicitWidth: parent.vertical ? 1 : 25
                    implicitHeight: parent.vertical ? 25 : 1
                    color: "#c3c3c3"
                }
}
